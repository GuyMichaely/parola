name: Persist default remote endpoint

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/persist-default-endpoint.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Apply source update
        run: python3 .github/scripts/persist-default-endpoint.py

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Build Parola
        working-directory: web
        run: |
          npm ci
          npm run build

      - name: Commit source update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add web/src/App.tsx web/src/storage.ts
          git commit -m "Persist remote endpoint independently of storage mode"
          git push
